【要求记忆】—— 用户对项目要求的特别记忆（可随时补充/修改）

1. 项目代号 / 文件夹名：Argus
2. 整体架构：前后端分离全栈项目
   - 前端：Vue 3（Vite + Pinia + Vue Router + Axios）
   - 后端：FastAPI（Uvicorn + SQLAlchemy + Pydantic）
3. 数据存储：
   - 关系库：MySQL 8.0
   - 缓存：Redis 7
   - 向量库：Milvus 2.4（standalone，依赖 etcd + minio）
   - 以上统一用 Docker Compose 管理
4. 智能体（Agent）技术栈：
   - LangChain
   - LangGraph
   - OpenAI Agents SDK
   - LLM 默认走 OpenAI，需自备 OPENAI_API_KEY（也可配 OPENAI_BASE_URL 走兼容网关）
   - 三种框架统一实现 BaseAgent 接口（run(prompt) 异步）
5. 前端布局要求：
   - 使用带侧边栏的 Layout
   - 侧边栏菜单：① 对话（下拉含 a、b）② 设置
   - 对话为可展开/收起的下拉菜单，a、b 为子项
6. 当前阶段：项目骨架与基础设施已搭建完成（后端骨架/配置/连接封装/智能体占位/health 路由、前端侧边栏 Layout 均已就绪，后端语法校验与前端 lint 均通过）；业务功能（数据模型与建表/Alembic、真实对话接口、Milvus 向量检索、前端 a/b 接入后端）待实现。

补充区（用户可在此追加特别要求）：
- 目录已从 20260912133345 重命名为 Argus：旧目录仍保留，需关闭 IDE 后手动删除。
- docker-compose 已编排：mysql、redis、etcd、minio、milvus、attu（Milvus 可视化 UI）。
- 后端已就绪：/api/v1/health 健康检查路由；agents/base.py 定义 BaseAgent 抽象接口，langchain_agent / langgraph_workflow / openai_agent 三套占位实现。
- 前端已就绪：layouts/DefaultLayout.vue（对话含 a/b 下拉、设置两项）、views/chat/ChatA、ChatB、Settings 与嵌套路由，lint 0 errors。每当用户说要记忆时，将信息补充到下面
