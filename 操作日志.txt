【操作日志】—— AI 助手操作简略记录

2026-09-12
- 初始化项目骨架：后端 FastAPI（app/core、api、models、schemas、services、agents、utils、tests）+ 前端 Vue3（src/{api,router,stores,views,layouts}）。
- 编写 requirements.txt：fastapi/uvicorn/sqlalchemy/pymysql/redis/pymilvus/langchain/langchain-openai/langgraph/openai-agents 等。
- 编写 docker-compose.yml：mysql、redis、etcd、minio、milvus、attu（Milvus UI）。
- 后端连接封装：core/config.py（pydantic-settings 统一配置）、database.py（MySQL+SQLAlchemy）、redis.py、milvus.py。
- 后端智能体：agents/base.py（BaseAgent 抽象接口）+ langchain_agent.py、langgraph_workflow.py（StateGraph 占位）、openai_agent.py（OpenAI Agents SDK 占位）。
- 后端路由：api/routes/health.py（/api/v1/health）。
- 后端 Python 语法校验通过（python -m compileall 无报错）。
- 前端侧边栏 Layout：新增 layouts/DefaultLayout.vue（对话含 a/b 下拉、设置两项菜单）；新增 views/chat/ChatA.vue、ChatB.vue、Settings.vue；调整 router 为嵌套路由 + 精简 App.vue。
- 前端 lint 校验通过（0 errors）。
- 文件夹重命名：用户要求将 20260912133345 改为 Argus；因当前 IDE 锁定源目录无法直接 Rename-Item，改用 robocopy 复制为 Argus（60 文件 / 30 目录，源目录保留待删）。
- 创建本《要求记忆.txt》与《操作日志.txt》于 Argus 根目录。
- 重写 src/views/chat/ChatA.vue 为千问风格聊天界面：消息气泡（用户右 / AI 左）、欢迎空状态与建议问题、底部输入栏（Enter 发送 / Shift+Enter 换行 / 自适应高度）、打字机式模拟回复与加载动画；支持代码块与 **加粗** 渲染。后端 /chat 接口未实现前回退本地 mock。

待办 / 后续可能操作：
- 删除旧目录 20260912133345（需关闭 IDE 后手动删除）。
- 业务数据模型 + 建表/Alembic 迁移。
- 智能体真实对话接口 + Milvus 向量检索封装。
- 前端对话 a/b 接入后端智能体。
