# Argus 学习笔记 — 2026-09-13

> 来源：今日与 AI 助手关于 Argus 后端架构、类型检查、Git、记忆系统的对话。
> 目标：把"踩过的坑"和"想通的设计原则"沉淀成可复用的知识，供后续开发查阅。

---

## 1. 静态类型检查（basedpyright）常见报错与修法

| 报错规则 | 含义 | 修法 |
|---|---|---|
| `reportImplicitRelativeImport` | 包内用 `from app.xxx import` 被当成"隐式相对导入" | 给目录加 `__init__.py` 成为包，改用显式相对导入 `from ..core.xxx import` |
| `reportTypedDictNotRequiredAccess` | 用下标访问 TypedDict 的 `NotRequired` 键不安全 | 改用 `.get()` |
| `reportUnknown*` / `reportUnusedCallResult` | fixture 参数无类型注解，导致成员类型全变"未知"的连锁告警 | 给 fixture 参数加注解（`pytest.MonkeyPatch` / `Path`）；未使用的返回值用 `_ =` 接收 |
| `reportMissingTypeStubs` / `reportMissingModuleSource` | 依赖未安装或没有类型存根 | 安装已在 `requirements.txt` 声明但未装的依赖（如 `pymilvus`，2.4+ 自带类型信息） |

**要点**：filter/链式调用的"未知类型"告警，往往是**上游某个参数缺注解**引起的连锁反应，不必逐个压制，补上根因注解即可清零。

---

## 2. Git / .gitignore 的 Windows 中文编码坑

- **现象**：`.gitignore` 里写中文文件名规则不生效，`git check-ignore` 匹配不到。
- **根因**：git-for-Windows 对**无 BOM** 的 `.gitignore` 会用系统 ANSI 编码（中文 Windows 为 GBK）读取，而文件名是 UTF-8，字节对不上 → 不匹配。
- **修法**：给 `.gitignore` 加 **UTF-8 BOM**（`EF BB BF`）即可让 git 按 UTF-8 读取。
- **已被跟踪的文件不会被忽略**：若文件此前已被 `git add`，需 `git rm --cached 文件名` 取消跟踪（保留磁盘文件），忽略规则才生效。
- **验证**：`git check-ignore -v 文件名` 能看到匹配到的规则行即成功。

---

## 3. FastAPI 后端分层架构（核心重点）

### 3.1 各目录职责

```
core/      只管"对外部服务的连接封装"（llm / redis / database / milvus / mcp / config）
utils/     把 core 的资源桥接成 FastAPI 依赖（get_redis / get_db，从 app.state 读取）
agents/    智能体编排 + agents/tools/（本地工具）
prompts/   提示词模板（与 agents 平级，被多方复用）
services/  业务逻辑（tool / 记忆 的存储实现都在这，调用 core 客户端）
models/    数据库表结构（ORM，继承 Base）
schemas/   请求/响应数据校验（Pydantic，不参与业务）
api/       HTTP 入口
main.py    启停接线（lifespan 里 connect、shutdown 里 close）
```

### 3.2 SQLAlchemy：Session 与 Model

- `Base(DeclarativeBase)` 是所有 ORM 模型的基类，模型继承它即被登记进 `Base.metadata`。
- `get_db()` 产出的是**通用 `AsyncSession`**——一支笔能写任意表格，不随表变化。
- **"每个表字段不同"由 Model 在 `models/` 各自定义一次吸收掉**，后续 session / 业务代码不用关心字段差异。
- **不需要为每个表单独写连接/业务样板**：业务写在 `services/`，按"功能"而非"表"组织，可跨多张表。

### 3.3 Redis 连接与操作分层

- `core/redis.py` 的 `RedisManager`：**有状态的连接池管理**（建池 / 关池），对应常驻连接。
- `utils/redis_util.py` 的 `get_redis`：`aioredis.Redis(connection_pool=...)` 把"池"包成**能执行命令的客户端**，通过依赖注入进路由。
- `aioredis.Redis(connection_pool=pool)` 的意义：池本身不会说话，包一层 client 才能 `await r.set/get`。
- **连接生命周期**：每个请求 new 一个轻量 client 对象（只是借池的引用，零成本），**不手动 close**；真正的 TCP 连接在 `app.state` 的池里，启动建、关停关。
- **操作放哪**：
  - 简单偶发的 `redis.get/set` → 路由里直接用注入的客户端；
  - 复杂或高频复用的逻辑 → 抽到 `services/`（如 `cache.py`），把 `redis` 当参数传进去。

### 3.4 LLM / 智能体分层

- `core/llm.py` 的 `get_chat_model(provider)`：返回 LangChain `ChatModel` 的**工厂函数**，解耦"模型怎么连"与"智能体怎么用"。
- **LLM 客户端是无状态、轻量的** → 用工厂函数即可（同 `core/milvus.py` 的 `get_milvus_client`），**不必**像 Redis/MySQL 那样做成带 `connect/close` 的类（它们有连接池要管）。
- `agents/tools/` 放本地工具（如 `call_api`、`search_db`）；工具本身不写 ORM，应**委托 `services/` 层**。
- `core/mcp.py` 管"外部 MCP server 工具"（如百度搜索），与本地工具区分开，二者都喂给同一 agent。
- 提示词建议放 `prompts/`（与 `agents/` 平级）：多方复用、import 干净；若确定只有 agents 用，放 `agents/prompts.py` 也可。

---

## 4. 分层记忆（Tiered Memory）架构

将对话记忆按"时效 + 检索方式"分三层：

```
① 短期记忆  → Redis     最近 N 条（滚动窗口，如 20 条，快、临时）
       │ 满阈值触发压缩
       ▼
   调 LLM 摘要 + 抽取重要信息
       ├─▶ ② 长期结构化  → MySQL    明确事实（可精确查询/筛选）
       └─▶ ③ 语义记忆     → Milvus   对话摘要的向量（按"意思"检索，供 RAG）
```

**要点**：
- **MySQL vs Milvus 分工**：MySQL 存"要精确查的事实"（如用户偏好），Milvus 存"按语义召回的历史摘要"。二者用途不同，可同时用。
- **压缩后别失忆**：摘要要作为一条"合成消息"留在下一轮 Redis 窗口头部，否则 agent 丢失前情。
- **触发方式**：放在 `services/memory.py` 里按条数/ token 自动触发，比让 agent 自己决定更稳。
- **检索时拼回 prompt**：新对话 = Redis 最近窗口 + MySQL 该用户事实 + Milvus 相关历史摘要。

---

## 5. 重构纪律（重命名一致性）

- 改类名必须同步：类定义、`import`、类型注解、实例化、以及 `app.state` 上的属性名，全仓搜索确认无残留。
- **保留返回 session 的函数名**（`get_db` / `get_redis`）：它们返回的是会话级对象，不是 Manager 实例；改名会牵动路由 import，无意义。
- 实例变量名与类名保持一致（`redis_manager` / `database_manager`），`app.state` 上的属性名也同步，避免"类叫 Manager、变量叫 client"的违和。

---

## 附：本项目约定速查

- 连接管理：`core/*Manager` 类（connect/close，常驻池）；`utils/*_util.py` 提供依赖注入（读 `app.state`）。
- `main.py` 的 `lifespan` 是唯一负责启停的地方：启动 connect、关闭 close。
- 业务永远在 `services/`，`core` 不写业务，`agents/tools` 也不写 ORM（委托 services）。
- 已有 git 忽略项：`操作日志.txt`、`要求记忆.txt`（注意 `.gitignore` 带 UTF-8 BOM 才能正确匹配中文）。
